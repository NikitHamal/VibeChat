// IMPORTANT: This is an example file.
// 1. Create a copy of this file and name it `firebase-config.js`.
// 2. This new file (`firebase-config.js`) is already in .gitignore to protect your keys.
// 3. Get your web app's Firebase configuration from your Firebase project settings and paste it here.

const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  databaseURL: "YOUR_DATABASE_URL",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_STORAGE_BUCKET",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID"
};

// Export the configuration for use in other modules
export default firebaseConfig;